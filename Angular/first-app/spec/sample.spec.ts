describe('Sample Jasmine Test', () => {
  it('should add numbers correctly', () => {
    expect(1 + 2).toBe(3);
  });

  it('should subtract numbers correctly', () => {
    expect(5 - 2).toBe(3);
  });
});
